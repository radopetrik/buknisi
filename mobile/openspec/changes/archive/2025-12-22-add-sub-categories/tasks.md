## 1. Database Implementation
- [x] 1.1 Create migration to add `sub_categories` table (id, name, slug, category_id).
- [x] 1.2 Add `sub_category_id` to `companies` table.
- [x] 1.3 Seed initial sub-categories (if required).

## 2. API / Backend (if applicable, currently Supabase direct)
- [x] 2.1 Update Typescript types for Database schema.
