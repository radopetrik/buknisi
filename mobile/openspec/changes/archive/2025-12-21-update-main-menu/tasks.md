## 1. Implementation
- [x] 1.1 Create new `app/(tabs)/home.tsx` screen (Implemented as `index.tsx` for default route)
- [x] 1.2 Update `app/(tabs)/_layout.tsx`:
    - Add `home` route as the first tab
    - Update `index` (or rename to `explore`) label to "Prehliadať"
    - Verify `bookings` label is "Rezervácie"
    - Verify `profile` label is "Profil"
- [x] 1.3 Set "Home" as the initial route/tab if necessary
- [x] 1.4 Add appropriate icons for Home (e.g., Home icon) and Explore (e.g., Search/Compass)
